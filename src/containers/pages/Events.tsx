import React, { FC } from 'react';

import Events from 'components/pages/Events';

const EnhancedEvents: FC = () => {
  // eslint-disable-next-line react/jsx-props-no-spreading
  return <Events />;
};

export default EnhancedEvents;
